/** Sections hidden from area-level editing because they are configured per port. */
export const AREA_SET_HIDDEN_SECTION_IDS: readonly string[] = [
  'pilotage',
  'tug',
  'moor',
  'garbage',
]

export const PORT_OVERRIDE_VISIBLE_SECTION_IDS: readonly string[] = [
  'pilotage',
  'tug',
  'moor',
  'garbage',
]
